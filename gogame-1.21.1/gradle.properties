# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
org.gradle.jvmargs=-Xmx1G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

# Local proxy (e.g. Clash) is machine-specific, so the active configuration lives in the
# user-wide file ~/.gradle/gradle.properties (on Windows: %USERPROFILE%\.gradle\gradle.properties)
# to keep it out of version control and avoid breaking CI (.github/workflows/build.yml).
# To scope the proxy to this project only, uncomment the lines below (they take precedence
# over the user-wide file):
#systemProp.http.proxyHost=127.0.0.1
#systemProp.http.proxyPort=7897
#systemProp.https.proxyHost=127.0.0.1
#systemProp.https.proxyPort=7897
#systemProp.http.nonProxyHosts=localhost|127.0.0.1
#systemProp.https.nonProxyHosts=localhost|127.0.0.1

#read more on this at https://github.com/neoforged/ModDevGradle?tab=readme-ov-file#better-minecraft-parameter-names--javadoc-parchment
# you can also find the latest versions at: https://parchmentmc.org/docs/getting-started
parchment_minecraft_version=1.21.1
parchment_mappings_version=2024.11.17
# Environment Properties
# You can find the latest versions here: https://projects.neoforged.net/neoforged/neoforge
# The Minecraft version must agree with the Neo version to get a valid artifact
minecraft_version=1.21.1
# The Minecraft version range can use any release version of Minecraft as bounds.
# Snapshots, pre-releases, and release candidates are not guaranteed to sort properly
# as they do not follow standard versioning conventions.
minecraft_version_range=[1.21.1]
# The Neo version must agree with the Minecraft version to get a valid artifact
# mcphone 1.9.3 requires NeoForge 21.1.200+. We compile against 21.1.250 (already
# cached & build-verified on this machine). deepseek pins 21.1.248, but its userdev.jar
# would not download here (the proxy terminated the TLS handshake); the 2-patch gap is
# negligible because we only touch mcphone's api package + stable vanilla API, never a
# 250-only API. neo_version_range below is the real player-compatibility guard.
neo_version=21.1.250
# Runtime NeoForge range accepted, written into neoforge.mods.toml. Kept wider than
# neo_version so upgrading the dev environment does not lock out lower-version players.
neo_version_range=[21.1.200,)
# The loader version range can only use the major version of FML as bounds
loader_version_range=[1,)

## Mod Properties

# The unique mod identifier for the mod. Must be lowercase in English locale. Must fit the regex [a-z][a-z0-9_]{1,63}
# Must match the String constant located in the main mod class annotated with @Mod.
mod_id=gogame
# The human-readable display name for the mod.
mod_name=GoGame
# The license of the mod. Review your options at https://choosealicense.com/. All Rights Reserved is the default.
mod_license=All Rights Reserved
# The mod version. See https://semver.org/
mod_version=1.0.0
# The group ID for the mod. It is only important when publishing as an artifact to a Maven repository.
# This should match the base package used for the mod sources.
# See https://maven.apache.org/guides/mini/guide-naming-conventions.html
mod_group_id=com.november.gogame

## MCphone body (hard prerequisite)
#
# No public maven for the body, so we compile against the jar in libs/ (the same route
# docs/addon-api.md recommends). This number is used twice and MUST stay identical:
# to build the libs/ filename, and as the lower bound in neoforge.mods.toml's
# versionRange. 1.9.3 = mcphone API VERSION 1 (no opensInsidePhone()).
mcphone_version=1.9.3
